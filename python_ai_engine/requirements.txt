# Python AI Engine - Phase 1~4
# Python 3.8+ required

# ==================== Phase 1 (MVP) ====================
# No external dependencies — standard library only

# ==================== Phase 2 (Core Infrastructure) ====================
numpy>=1.21.0              # 수치 연산, 분석 서비스

# ==================== Phase 3 (Extensibility) ====================
# Plugin system, sandbox — no extra deps

# ==================== Phase 4 (Advanced Services) ====================
# --- Model Loading ---
# onnxruntime>=1.14.0      # ONNX 모델 추론 (선택)
# scikit-learn>=1.0.0      # sklearn 모델 로딩 (선택)
# joblib>=1.1.0            # sklearn 모델 직렬화 (선택)

# --- Vision Services ---
# ultralytics>=8.0.0       # YOLOv8 객체 탐지 (선택)
# easyocr>=1.6.0           # OCR 텍스트 인식 (선택)
# opencv-python>=4.7.0     # 이미지 처리 (선택)

# --- Analysis ---
# pandas>=1.3.0            # 데이터 분석 (선택)
# scipy>=1.7.0             # 과학 연산 (선택)

# ==================== 설치 안내 ====================
# 기본 설치 (Phase 1-3, 분석 포함):
#   pip install numpy
#
# 모델 추론 추가:
#   pip install onnxruntime scikit-learn joblib
#
# Vision 기능 추가:
#   pip install ultralytics easyocr opencv-python
#
# 전체 설치:
#   pip install numpy onnxruntime scikit-learn joblib ultralytics easyocr opencv-python pandas scipy
