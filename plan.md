# SCADA Schedule UX 개선 계획

## 현재 문제점 분석

### 기존 구조 (5개 분리된 다이얼로그)
현재 스케쥴 설정은 다음과 같이 **5개의 독립 다이얼로그**로 나뉘어 있어 사용자가 전체 흐름을 이해하기 어렵다:

1. **FormConfigSchedule** (F3) - 고정 스케쥴 관리 (스케쥴명 + 색상 + 모델 할당)
2. **FormConfigScheduleAdditional** (F4) - 추가 스케쥴 관리 (특수일/공휴일/매일)
3. **FormConfigModel** (F6) - 모델 편집 (시간 + 태그값 + 스크립트)
4. **FormConfigScheduleWeek** (F7) - 요일별 고정스케쥴 매핑
5. **FormConfigScheduleDesigner** (F8) - 통합 디자이너 (최근 추가, 25-03-11)

### 핵심 UX 문제점

| 문제 | 상세 |
|------|------|
| **개념 이해 난이도** | Schedule → Model → Model Item → Tag/Script 의 4단계 계층을 이해해야 설정 가능 |
| **분산된 설정 경로** | 하나의 완전한 스케쥴을 만들려면 최소 3개 다이얼로그를 오가야 함 |
| **요일 매핑 별도** | 고정스케쥴을 만든 후 별도로 요일 매핑 다이얼로그를 열어야 함 |
| **모델 관리 분리** | 모델을 먼저 만들어야 스케쥴에 할당 가능 (순서 종속성) |
| **즉시 미리보기 없음** | 설정 후 실제 어떤 시간에 무엇이 실행되는지 미리보기가 없음 |
| **Designer 미완성** | 최근 추가된 Designer(F8)가 3탭으로 통합했지만 여전히 복잡 |

---

## 방안 1: 기존 FormConfigScheduleDesigner 기반 UX 극대화 (우선 방안)

기존 `FormConfigScheduleDesigner`를 개선하여 **설정 흐름을 가이드하는 위자드 방식**으로 변경.
기존 데이터 구조와 저장 형식을 100% 유지하면서 UI만 개선한다.

### 1-A. 탭 구조 재편 + 가이드 패널 추가

**현재 탭:**
- Tab1: 고정 스케쥴 + 요일매핑 + 모델 ON/OFF
- Tab2: 추가 스케쥴
- Tab3: 공휴일/특수일

**개선 후 탭 (4탭):**
- **Tab1: 요일 스케쥴 (주간 뷰)** ← 가장 직관적인 진입점
  - 요일별 캘린더 그리드 (월~일 + 공휴일 + 특수일)
  - 각 요일 클릭 시 할당된 고정스케쥴 표시 + 변경 가능
  - **하단에 "오늘의 실행 예정" 미리보기 패널** 추가
- **Tab2: 고정 스케쥴 관리**
  - 좌측: 스케쥴 목록 (색상 표시)
  - 우측: 해당 스케쥴의 모델 리스트 (Enable/Disable)
  - **개선: 모델 추가 시 인라인 "빠른 모델 생성" 버튼**
- **Tab3: 추가 스케쥴** (기존 유지 + 미리보기 추가)
- **Tab4: 공휴일/특수일** (기존 유지)

### 1-B. 핵심 UX 개선 항목

#### (1) 주간 타임라인 미리보기 (Tab1에 추가)
```
┌─────────────────────────────────────────────┐
│  주간 스케쥴 미리보기                          │
│                                             │
│  월 [평일운전] ▶ 06:00 시작 | 18:00 정지    │
│  화 [평일운전] ▶ 06:00 시작 | 18:00 정지    │
│  수 [평일운전] ▶ 06:00 시작 | 18:00 정지    │
│  목 [평일운전] ▶ 06:00 시작 | 18:00 정지    │
│  금 [평일운전] ▶ 06:00 시작 | 18:00 정지    │
│  토 [휴일운전] ▶ 08:00 시작 | 14:00 정지    │
│  일 [휴일운전] ▶ -없음-                      │
└─────────────────────────────────────────────┘
```
- `Schedule.GetScheduleModelStruct()` + `ScheduleLib.ModelLoad()`를 활용하여 각 요일의 고정스케쥴에 연결된 모델 아이템의 시간/액션을 표시
- 사용자가 "이 설정이 실제로 어떻게 동작하는지" 즉시 확인 가능

#### (2) 고정 스케쥴 추가 시 자동 요일 매핑 제안
- 고정 스케쥴 생성 후 "이 스케쥴을 어떤 요일에 적용하시겠습니까?" 다이얼로그 팝업
- 체크박스로 요일 선택 → 자동으로 `WEEK.LSTX`에 반영

#### (3) 모델 인라인 편집
- 현재: 모델을 별도 다이얼로그에서 먼저 생성해야 함
- 개선: 고정 스케쥴 내에서 "모델 새로 만들기" 버튼 → 모델 편집 다이얼로그 열림 → 완료 후 자동으로 해당 스케쥴에 연결

#### (4) 상태 표시 강화
- FormConfigScheduleDesigner 상단에 현재 스케쥴 활성/비활성 상태 표시
- `CheckEngineSchedule.bActiveSchedule` 값을 읽어 "스케쥴 활성" / "스케쥴 비활성" 표시

### 1-C. 구현 파일 목록

| 파일 | 변경 내용 |
|------|----------|
| `FormConfigScheduleDesigner.cs` | Tab1 재설계(주간 뷰), 미리보기 패널, 자동 요일매핑 제안, 상태 표시 |
| `FormConfigScheduleDesigner.resx` | 새 컨트롤 리소스 |
| `FormConfigScheduleDesigner.ko.resx` | 한국어 리소스 |
| `FormSchedule.cs` | Designer를 기본 진입점으로 변경 (F8 → F3 또는 메인 버튼) |
| `ScheduleLib.cs` | `GetWeeklyPreview()` 헬퍼 메서드 추가 (미리보기 데이터 조회) |

### 1-D. 구현 단계

1. **ScheduleLib에 미리보기 헬퍼 메서드 추가**
   - 요일별 스케쥴명 + 연결된 모델의 시간/액션 목록을 반환하는 메서드

2. **FormConfigScheduleDesigner Tab1을 주간 뷰로 재설계**
   - 기존 Tab1의 요일 ComboBox를 유지하되 상단에 주간 타임라인 ListView 추가
   - 요일 ComboBox 변경 시 미리보기 자동 갱신

3. **고정 스케쥴 추가 후 요일 매핑 제안 다이얼로그**
   - `buttonScheduleAdd_Click` 에서 스케쥴 생성 후 요일 매핑 제안

4. **모델 인라인 생성 버튼 추가**
   - Tab1의 모델 영역에 "새 모델 생성" 버튼 추가

5. **FormSchedule 진입점 조정**
   - Designer를 기본/우선 설정 경로로 승격

---

## 방안 2: 새로운 통합 스케쥴 에디터 (대안)

방안 1로 충분한 UX 개선이 되지 않을 경우, 완전히 새로운 Form을 작성하는 방안.

### 컨셉: "캘린더 중심 올인원 에디터"

```
┌─────────────────────────────────────────────────────┐
│ [스케쥴 디자이너]                          [ON/OFF] │
├──────────────┬──────────────────────────────────────┤
│ 3월 2026      │  2026년 3월 17일 (화) - 평일운전     │
│ 월 화 수 목 금 토 일│                                  │
│  ... 캘린더 ... │  ┌ 06:00 [시작] 태그1=ON         │
│  17일 ← 선택   │  ├ 12:00 [점검] 태그2=CHECK       │
│                │  └ 18:00 [정지] 태그1=OFF         │
│                │                                    │
│ ─────────────│  [모델 편집] [모델 추가]             │
│ 고정 스케쥴:    │──────────────────────────────────────│
│ ■ 평일운전     │ 요일 매핑:                          │
│ ■ 휴일운전     │ 월~금: 평일운전                     │
│ ■ 특수운전     │ 토~일: 휴일운전                     │
│                │ 공휴일: 휴일운전                     │
│ [추가][삭제]    │ [요일매핑 변경]                      │
└──────────────┴──────────────────────────────────────┘
```

- 좌측: 캘린더 + 고정스케쥴 목록
- 우측: 선택된 날짜의 실행 타임라인 + 요일 매핑
- 기존 데이터 구조/파일 형식 100% 호환

### 방안 2 구현시 추가 파일
- `FormScheduleEditorNew.cs` (새 통합 에디터)
- 기존 다이얼로그는 레거시로 유지 (호환성)

---

## 권장 사항

**방안 1 우선 적용을 권장합니다.**

이유:
- 기존 `FormConfigScheduleDesigner`가 이미 3탭 통합 구조를 갖추고 있음 (25-03-11 추가)
- Tab1에 주간 미리보기만 추가해도 사용성이 크게 향상
- 기존 코드 변경 최소화로 안정성 확보
- 데이터 구조 변경 없음 → 기존 프로젝트 호환성 유지
- 점진적 개선 가능 (미리보기 → 인라인 모델 생성 → 자동 매핑 순)
